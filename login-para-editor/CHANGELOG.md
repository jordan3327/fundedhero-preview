# Changelog

Todos los cambios notables de este paquete.

## [1.0.0] - 2026-09-19

### Añadido

- Paquete separado desde `fundedhero-preview/app/index.html` (133 líneas, 0 conflictos).
- `assets/css/modern-login.css`, `assets/js/modern-login.js`, imágenes `favicon.png` / `logo_auth.png`.
- `dist/login-standalone.html`: build de 1 archivo (CSS+JS inline) para preview.
- `docs/INTEGRATION.md` + `docs/QA-CHECKLIST.md`.
- `.editorconfig`, `package.json` (serve), `assets/fonts/.gitkeep`.

### Organización

- Estructura `assets/{css,js,images,fonts}`, `docs/`, `dist/`.
- `index.html` con metadatos (`description`, `theme-color`) y comentarios por sección. Sin cambios de texto/IDs/funciones.
